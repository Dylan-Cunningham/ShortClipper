# ShortClipper Backend

Local API for parsing clip plans, running ffmpeg, and browsing project files.

## Run

```powershell
cd D:\Dylan\projects\ShortClipper\Backend
npm.cmd install
npm.cmd run dev
```

Default API URL: `http://localhost:8787`
